<!DOCTYPE html>
<html>
<head>
  <title>My Project</title>
</head>
<body>
  <h1>Items</h1>
  <ul>
    <% items.forEach(function(item) { %>
      <li><%= item.name %> - <%= item.description %></li>
    <% }); %>
  </ul>
</body>
</html>